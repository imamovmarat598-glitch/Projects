# ⚡️ Быстрый старт за 5 минут

## Шаг 1: Создайте Telegram бота (2 минуты)

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте `/newbot`
4. Введите имя: `City Travel Helper`
5. Введите username: `city_travel_helper_bot` (или любой другой, заканчивающийся на `bot`)
6. **СКОПИРУЙТЕ ТОКЕН** (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Настройте бота (1 минута)

1. Откройте файл `.env` в папке проекта
2. Вставьте токен:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_сюда
```

3. Сохраните файл

## Шаг 3: Запустите бота (2 минуты)

Откройте терминал в папке `city-travel-bot` и выполните:

```bash
npm install
npm run dev
```

Вы увидите:
```
✅ Bot launched successfully
```

## 🎉 Готово! Бот работает!

Откройте Telegram, найдите своего бота по username и отправьте `/start`

---

## 🚀 Что дальше?

### Без базы данных (работает сейчас)
Бот работает, но:
- ❌ Нет сохранения пользователей
- ❌ Нет истории поисков
- ❌ Нет системы подписок

### С базой данных (рекомендуется)

**Windows:**
1. Скачайте PostgreSQL: https://www.postgresql.org/download/windows/
2. Установите (запомните пароль!)
3. Откройте pgAdmin или командную строку
4. Создайте базу: `CREATE DATABASE city_travel_bot;`

**Mac:**
```bash
brew install postgresql
brew services start postgresql
createdb city_travel_bot
```

**Linux:**
```bash
sudo apt-get install postgresql
sudo -u postgres createdb city_travel_bot
```

**Затем:**
```bash
# В файле .env укажите:
DATABASE_URL=postgresql://postgres:ваш_пароль@localhost:5432/city_travel_bot

# Запустите миграции:
npm run build
npm run db:migrate
```

---

## 🔧 Опциональные настройки

### Yandex Maps (для маршрутов)
1. Регистрация: https://developer.tech.yandex.ru/
2. Создайте API ключ
3. Добавьте в `.env`:
```env
YANDEX_MAPS_API_KEY=ваш_ключ
```

### Платежи (для монетизации)
1. Выберите провайдера: ЮMoney, CloudPayments, Stripe
2. Получите токен
3. Настройте через @BotFather → Payments
4. Добавьте в `.env`:
```env
PAYMENT_PROVIDER_TOKEN=ваш_токен
```

---

## ❓ Проблемы?

**Ошибка: "TELEGRAM_BOT_TOKEN is required"**
→ Проверьте файл `.env`, токен должен быть без пробелов

**Ошибка: "Cannot find module"**
→ Запустите `npm install`

**База данных не подключается**
→ Бот работает БЕЗ БД! Просто функционал ограничен

**Бот не отвечает**
→ Убедитесь что в терминале написано "Bot launched successfully"

---

## 📱 Тестирование бота

1. Откройте своего бота в Telegram
2. Отправьте `/start`
3. Нажмите "🏙 Выбрать город"
4. Выберите "Москва"
5. Выберите "1 день"
6. Нажмите "🎭 Афиша"
7. Получите список событий!

---

**Поздравляем! Ваш бот работает! 🎉**
