# ⚡️ БЫСТРЫЙ СТАРТ: После получения credentials.json

## Что делать когда у вас есть credentials.json

### Шаг 1: Разместите файл credentials.json

```bash
# Скопируйте скачанный credentials.json в папку проекта
# Должна быть такая структура:

city-travel-bot/
  credentials.json          ← ВОТ ЗДЕСЬ (рядом с package.json)
  package.json
  src/
  .env
```

### Шаг 2: Обновите .env

Откройте файл `.env` и добавьте ID вашей Google Таблицы:

```env
GOOGLE_SHEETS_ID=ваш_id_таблицы_здесь
```

**Где взять ID таблицы:**
1. Откройте вашу Google Таблицу
2. Посмотрите на URL:
   ```
   https://docs.google.com/spreadsheets/d/1a2b3c4d5e6f7g8h9i0j/edit
                                         ^^^^^^^^^^^^^^^^^^^^
                                         Это ваш ID
   ```
3. Скопируйте эту часть и вставьте в `.env`

### Шаг 3: Установите новые зависимости

```bash
cd city-travel-bot
npm install
```

Это установит:
- `googleapis` - для работы с Google Sheets API
- `google-auth-library` - для авторизации

### Шаг 4: Дайте доступ Service Account к таблице

1. Откройте файл `credentials.json`
2. Найдите строку с `"client_email"`
3. Скопируйте этот email (выглядит как `name@project.iam.gserviceaccount.com`)
4. Откройте вашу Google Таблицу
5. Нажмите кнопку **"Настроить доступ"** (Share)
6. Вставьте скопированный email
7. Дайте права **"Редактор"** (Editor)
8. Уберите галочку "Уведомить людей"
9. Нажмите **"Поделиться"**

### Шаг 5: Соберите и запустите бота

```bash
npm run build
npm run dev
```

### Шаг 6: Проверьте что всё работает

**В консоли вы должны увидеть:**
```
✅ Google Sheets подключен
📋 Создан лист: Users
📋 Создан лист: History
📋 Создан лист: Favorites
📋 Создан лист: Preferences
✅ Bot launched successfully
✅ City Travel Bot is running!
```

**В Google Таблице должны появиться 4 листа:**
- Users (с заголовками telegram_id, username, ...)
- History (с заголовками user_id, city, ...)
- Favorites (с заголовками user_id, city, ...)
- Preferences (с заголовками user_id, event_types, ...)

### Шаг 7: Протестируйте бота

1. Откройте Telegram
2. Найдите своего бота
3. Отправьте `/start`
4. Выберите город и дату
5. Получите афишу

**После этого:**
- Откройте Google Таблицу
- Перейдите на лист **Users**
- Вы должны увидеть свою запись с вашим Telegram ID

---

## ✅ Чеклист проверки

- [ ] `credentials.json` находится в корне проекта
- [ ] `GOOGLE_SHEETS_ID` добавлен в `.env`
- [ ] Зависимости установлены (`npm install`)
- [ ] Service Account email добавлен в доступ к таблице
- [ ] Бот собран и запущен (`npm run build && npm run dev`)
- [ ] В консоли появилось "Google Sheets подключен"
- [ ] В таблице создались 4 листа
- [ ] Протестировали `/start` в боте
- [ ] Данные появились в листе Users

---

## ❌ Возможные ошибки

### Ошибка: "credentials.json не найден"

**Решение:**
```bash
# Проверьте что файл на месте
ls credentials.json

# Должен быть в корне проекта, не в src/
```

### Ошибка: "403 Forbidden" или "Permission denied"

**Решение:**
1. Откройте `credentials.json`
2. Скопируйте `client_email`
3. Добавьте его в настройки доступа Google Таблицы с правами "Редактор"

### Ошибка: "GOOGLE_SHEETS_ID is not defined"

**Решение:**
Добавьте в `.env`:
```env
GOOGLE_SHEETS_ID=ваш_id_таблицы
```

### Ошибка: "Cannot find module 'googleapis'"

**Решение:**
```bash
npm install googleapis google-auth-library
```

### В таблице не появляются данные

**Решение:**
1. Проверьте логи бота - должно быть "Google Sheets подключен"
2. Проверьте что Service Account имеет доступ к таблице
3. Проверьте что `GOOGLE_SHEETS_ID` правильный

---

## 🚀 После успешного запуска

Когда бот работает с Google Sheets, вы можете:

1. **Просматривать пользователей** - лист Users
2. **Смотреть историю поисков** - лист History
3. **Управлять избранным** - лист Favorites
4. **Настраивать предпочтения** - лист Preferences

**Следующие шаги:**
- Настроить N8N для автоматизации
- Интегрировать OpenAI для персонализации
- Добавить систему уведомлений
- Настроить платежи через СБП

---

## 📞 Нужна помощь?

Если что-то не работает:

1. Проверьте все пункты в чеклисте выше
2. Посмотрите логи в консоли при запуске бота
3. Откройте [GOOGLE_SHEETS_SETUP.md](GOOGLE_SHEETS_SETUP.md) для полной инструкции
4. Проверьте что Google Sheets API включен в Cloud Console

---

**Всё готово!** Теперь бот хранит данные в Google Sheets вместо PostgreSQL! 🎉
