# 🏙 City Travel Bot - Telegram бот для путешественников

Telegram-бот для планирования поездок в города России. Получайте афишу событий, подбор отелей, маршруты и достопримечательности в одном месте.

## 🎯 Возможности

### Бесплатный тариф
- ✅ Доступ к 5 популярным городам (Москва, СПб, Казань, Екатеринбург, Сочи)
- ✅ 3 запроса в день
- ✅ Афиша концертов и событий (KudaGo API)
- ✅ Базовая информация об отелях

### 💎 Premium подписка (299₽/мес)
- ✅ Доступ ко всем городам России (100+)
- ✅ Неограниченные запросы
- ✅ Полный список отелей с ценами
- ✅ Персональные маршруты
- ✅ Экспорт в PDF/Google Maps
- ✅ Уведомления о новых событиях

### 👑 VIP подписка (999₽/мес)
- ✅ Все из Premium
- ✅ AI-рекомендации на основе предпочтений
- ✅ Эксклюзивные скидки на билеты и отели
- ✅ Консьерж-сервис

## 🚀 Быстрый старт

### Требования
- Node.js 18+
- PostgreSQL 14+ (опционально для полного функционала)
- Telegram Bot Token (получить через [@BotFather](https://t.me/BotFather))

### Установка

1. **Клонируйте репозиторий или перейдите в директорию:**
```bash
cd city-travel-bot
```

2. **Установите зависимости:**
```bash
npm install
```

3. **Настройте переменные окружения:**
```bash
cp .env.example .env
```

Отредактируйте файл `.env` и укажите:
```env
# Обязательные параметры
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Опциональные (бот работает без них)
DATABASE_URL=postgresql://localhost:5432/city_travel_bot
YANDEX_MAPS_API_KEY=your_api_key
PAYMENT_PROVIDER_TOKEN=your_payment_token
```

### 🤖 Создание Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: `City Travel Helper`)
   - Введите username бота (должен заканчиваться на `bot`, например: `city_travel_helper_bot`)
4. Скопируйте токен и вставьте в `.env` файл как `TELEGRAM_BOT_TOKEN`

### 📦 База данных (опционально)

Бот работает БЕЗ базы данных, но с ограниченным функционалом.

**Для полного функционала настройте PostgreSQL:**

1. **Установите PostgreSQL** (если еще не установлен)
   - Windows: [скачать](https://www.postgresql.org/download/windows/)
   - Mac: `brew install postgresql`
   - Linux: `sudo apt-get install postgresql`

2. **Создайте базу данных:**
```bash
# Войдите в PostgreSQL
psql -U postgres

# Создайте базу данных
CREATE DATABASE city_travel_bot;

# Выйдите
\q
```

3. **Запустите миграции:**
```bash
npm run build
npm run db:migrate
```

### ▶️ Запуск бота

**Режим разработки (с hot-reload):**
```bash
npm run dev
```

**Продакшн режим:**
```bash
npm run build
npm start
```

## 📁 Структура проекта

```
city-travel-bot/
├── src/
│   ├── bot/
│   │   ├── bot.ts              # Основная логика бота
│   │   └── keyboards.ts        # Клавиатуры и кнопки
│   ├── config/
│   │   └── config.ts           # Конфигурация приложения
│   ├── database/
│   │   ├── db.ts               # Операции с БД
│   │   ├── schema.sql          # SQL схема
│   │   └── migrate.ts          # Скрипт миграций
│   ├── services/
│   │   ├── kudago.service.ts   # Интеграция KudaGo API
│   │   ├── yandexMaps.service.ts # Yandex Maps API
│   │   └── payment.service.ts  # Платежная система
│   └── index.ts                # Точка входа
├── .env.example                # Пример конфигурации
├── package.json
├── tsconfig.json
└── README.md
```

## 🔧 Настройка API

### KudaGo API (афиша событий)
- Не требует ключа
- Работает сразу после запуска
- Документация: https://docs.kudago.com/api/

### Yandex Maps API (маршруты)
1. Зарегистрируйтесь на https://developer.tech.yandex.ru/
2. Создайте API ключ для Geocoder и Router API
3. Добавьте ключ в `.env` как `YANDEX_MAPS_API_KEY`

**Без ключа:** бот использует упрощенный расчет маршрутов

### Telegram Payments (монетизация)
1. Выберите платежного провайдера:
   - ЮMoney (Yandex): https://yookassa.ru/
   - CloudPayments: https://cloudpayments.ru/
   - Stripe (международные карты)
2. Получите токен провайдера
3. Свяжите провайдера с ботом через [@BotFather](https://t.me/BotFather)
   - Отправьте `/mybots`
   - Выберите своего бота
   - Payments → Выберите провайдера
4. Добавьте токен в `.env` как `PAYMENT_PROVIDER_TOKEN`

## 💻 Команды бота

- `/start` - Запуск бота, регистрация пользователя
- `/help` - Справка по использованию
- `/support` - Поддержка

**Меню:**
- 🏙 Выбрать город
- ⭐️ Избранное
- 💎 Premium подписка
- ⚙️ Настройки

## 🛠 Разработка

### Скрипты npm

```bash
npm run dev          # Запуск в режиме разработки с hot-reload
npm run build        # Компиляция TypeScript
npm start            # Запуск скомпилированного кода
npm run db:migrate   # Запуск миграций БД
```

### Добавление новых городов

Отредактируйте файл [src/bot/bot.ts](src/bot/bot.ts):

```typescript
const cities = ['Москва', 'Санкт-Петербург', 'Казань', /* добавьте города */];
```

И обновите клавиатуры в [src/bot/keyboards.ts](src/bot/keyboards.ts)

## 📊 База данных

### Таблицы

- **users** - Пользователи бота
- **search_history** - История поисков
- **payments** - Платежи и подписки
- **favorites** - Избранные города

### SQL запросы

Посмотреть пользователей:
```sql
SELECT * FROM users;
```

Посмотреть статистику подписок:
```sql
SELECT subscription_type, COUNT(*)
FROM users
GROUP BY subscription_type;
```

## 🚀 Деплой

### Heroku

1. Создайте приложение:
```bash
heroku create your-bot-name
```

2. Добавьте PostgreSQL:
```bash
heroku addons:create heroku-postgresql:hobby-dev
```

3. Установите переменные окружения:
```bash
heroku config:set TELEGRAM_BOT_TOKEN=your_token
```

4. Деплой:
```bash
git push heroku main
```

### VPS (Ubuntu/Debian)

1. Установите Node.js и PostgreSQL
2. Клонируйте репозиторий
3. Настройте `.env`
4. Запустите с PM2:
```bash
npm install -g pm2
npm run build
pm2 start dist/index.js --name city-travel-bot
pm2 save
pm2 startup
```

## 🔐 Безопасность

- ✅ Никогда не коммитьте `.env` файл
- ✅ Используйте переменные окружения для секретов
- ✅ Регулярно обновляйте зависимости: `npm audit fix`
- ✅ Используйте HTTPS для webhook (в продакшене)

## 📝 TODO

- [ ] Интеграция с Booking.com API для отелей
- [ ] Экспорт маршрутов в PDF
- [ ] Google Places API для достопримечательностей
- [ ] Реферальная программа
- [ ] Admin панель для статистики
- [ ] Telegram Mini App версия
- [ ] Мультиязычность (EN, RU)

## 🤝 Вклад в проект

Pull requests приветствуются! Для больших изменений сначала откройте issue для обсуждения.

## 📄 Лицензия

ISC

## 📞 Поддержка

Возникли вопросы? Создайте [Issue](https://github.com/your-repo/issues) или напишите в Telegram: [@your_username](https://t.me/your_username)

---

**Создано с ❤️ для путешественников**
