# 📊 Настройка Google Sheets для бота

## Пошаговая инструкция

### Шаг 1: Создайте Google Cloud проект

1. Откройте [Google Cloud Console](https://console.cloud.google.com/)
2. Нажмите **"Создать проект"** (или "Create Project")
3. Введите название: `city-travel-bot`
4. Нажмите **"Создать"**

### Шаг 2: Включите Google Sheets API

1. В левом меню выберите **"API и сервисы"** → **"Библиотека"**
2. Найдите **"Google Sheets API"**
3. Нажмите на него и выберите **"Включить"** (Enable)

### Шаг 3: Создайте Service Account

1. Перейдите в **"API и сервисы"** → **"Учетные данные"**
2. Нажмите **"Создать учетные данные"** → **"Аккаунт сервиса"**
3. Заполните:
   - **Имя:** `city-travel-bot-service`
   - **ID:** (автоматически)
   - **Описание:** `Service account for Telegram bot`
4. Нажмите **"Создать и продолжить"**
5. **Роль:** Выберите `Редактор` (Editor)
6. Нажмите **"Продолжить"** и затем **"Готово"**

### Шаг 4: Создайте JSON-ключ

1. В списке учетных записей найдите `city-travel-bot-service`
2. Нажмите на три точки (⋮) справа → **"Управление ключами"**
3. Нажмите **"Добавить ключ"** → **"Создать новый ключ"**
4. Выберите формат **JSON**
5. Нажмите **"Создать"**
6. Файл `credentials.json` автоматически скачается

### Шаг 5: Разместите credentials.json

```bash
# Переместите скачанный файл в папку проекта
# Он должен быть здесь:
city-travel-bot/
  credentials.json  ← ВОТ ЗДЕСЬ
  src/
  package.json
  ...
```

**⚠️ ВАЖНО:** Файл `credentials.json` содержит секретный ключ!
- Не публикуйте его в GitHub
- Файл уже добавлен в `.gitignore`

### Шаг 6: Создайте Google Таблицу

1. Откройте [Google Sheets](https://sheets.google.com/)
2. Нажмите **"Создать"** → **"Пустая таблица"**
3. Назовите таблицу: `City Travel Bot - Database`
4. Скопируйте **ID таблицы** из URL:
   ```
   https://docs.google.com/spreadsheets/d/[ЭТО_ID_ТАБЛИЦЫ]/edit
   ```

Пример:
```
https://docs.google.com/spreadsheets/d/1a2b3c4d5e6f7g8h9i0j/edit
                                      ^^^^^^^^^^^^^^^^^^^^
                                      Это ваш GOOGLE_SHEETS_ID
```

### Шаг 7: Дайте доступ Service Account к таблице

1. Откройте файл `credentials.json`
2. Найдите строку `"client_email": "city-travel-bot-service@..."`
3. Скопируйте этот email
4. Откройте вашу Google Таблицу
5. Нажмите **"Настроить доступ"** (Share)
6. Вставьте скопированный email
7. Выберите роль **"Редактор"** (Editor)
8. Снимите галочку **"Уведомить людей"**
9. Нажмите **"Поделиться"**

### Шаг 8: Настройте .env файл

Добавьте в `.env` ID вашей таблицы:

```env
# Google Sheets
GOOGLE_SHEETS_ID=1a2b3c4d5e6f7g8h9i0j
```

### Шаг 9: Установите зависимости

```bash
cd city-travel-bot
npm install googleapis google-auth-library
```

---

## 🎯 Проверка установки

### Тест 1: Проверьте credentials.json

```bash
# Файл должен существовать
ls credentials.json

# Проверьте структуру
cat credentials.json | grep "client_email"
```

Должны увидеть что-то вроде:
```json
"client_email": "city-travel-bot-service@city-travel-bot-123456.iam.gserviceaccount.com"
```

### Тест 2: Проверьте .env

```bash
cat .env | grep GOOGLE_SHEETS_ID
```

Должно быть:
```
GOOGLE_SHEETS_ID=ваш_id_таблицы
```

### Тест 3: Запустите бота

```bash
npm run build
npm run dev
```

Вы должны увидеть:
```
✅ Google Sheets подключен
📋 Создан лист: Users
📋 Создан лист: History
📋 Создан лист: Favorites
📋 Создан лист: Preferences
✅ Bot launched successfully
```

### Тест 4: Проверьте Google Таблицу

Откройте вашу таблицу - должны появиться 4 листа (вкладки):
- **Users** - с колонками telegram_id, username, first_name, ...
- **History** - с колонками user_id, city, date_from, ...
- **Favorites** - с колонками user_id, city, notify_new_events, ...
- **Preferences** - с колонками user_id, event_types, budget_level, ...

---

## 📋 Структура таблиц

### 1. Users (Пользователи)

| Колонка | Описание |
|---------|----------|
| telegram_id | Telegram ID пользователя |
| username | @username |
| first_name | Имя |
| last_name | Фамилия |
| subscription_type | free / premium / vip |
| subscription_expires | Дата окончания подписки |
| requests_today | Количество запросов сегодня |
| last_request_date | Дата последнего запроса |
| created_at | Дата регистрации |

### 2. History (История поисков)

| Колонка | Описание |
|---------|----------|
| user_id | Telegram ID пользователя |
| city | Название города |
| date_from | Дата начала поиска |
| date_to | Дата окончания поиска |
| created_at | Когда был запрос |

### 3. Favorites (Избранные города)

| Колонка | Описание |
|---------|----------|
| user_id | Telegram ID пользователя |
| city | Название города |
| notify_new_events | Уведомлять о новых событиях (true/false) |
| created_at | Когда добавлен в избранное |

### 4. Preferences (Предпочтения)

| Колонка | Описание |
|---------|----------|
| user_id | Telegram ID пользователя |
| event_types | Типы событий через запятую: concert,exhibition,theater |
| budget_level | low / medium / high |
| travel_style | cultural / party / family / romantic |
| preferences_text | Свободный текст (можно импортировать из Google Docs) |

---

## 🔧 Интеграция с ботом

После настройки Google Sheets нужно обновить бота:

### Обновить db-safe.ts

Вместо PostgreSQL и in-memory хранилища бот будет использовать Google Sheets.

```typescript
// src/bot/db-safe.ts
import { googleSheetsService } from '../services/googleSheets.service.js';

export const safeUserDb = {
  async findOrCreate(telegramId: number, userData: any) {
    return await googleSheetsService.findOrCreateUser(telegramId, userData);
  },

  async getByTelegramId(telegramId: number) {
    return await googleSheetsService.getUserByTelegramId(telegramId);
  },

  async checkRequestLimit(userId: number) {
    return await googleSheetsService.checkRequestLimit(userId);
  },

  async incrementRequests(userId: number) {
    await googleSheetsService.incrementRequests(userId);
  },

  async checkSubscription(userId: number) {
    const user = await googleSheetsService.getUserByTelegramId(userId);
    return user?.subscription_type || 'free';
  },

  async updateSubscription(userId: number, type: any, expiresAt: Date) {
    await googleSheetsService.updateSubscription(userId, type, expiresAt);
  },
};

export const safeSearchHistoryDb = {
  async add(userId: number, city: string, dateFrom: Date, dateTo: Date) {
    await googleSheetsService.addSearchHistory(userId, city, dateFrom, dateTo);
  },
};
```

### Инициализация при старте бота

```typescript
// src/index.ts
import { googleSheetsService } from './services/googleSheets.service.js';

async function main() {
  // Инициализируем Google Sheets
  const sheetsOk = await googleSheetsService.initialize();

  if (!sheetsOk) {
    console.log('⚠️  Бот работает без Google Sheets');
  }

  // Запускаем бота
  const bot = new CityTravelBot();
  await bot.launch();
}

main();
```

---

## 🎁 Преимущества Google Sheets

✅ **Не нужна установка PostgreSQL**
- Работает сразу после настройки
- Облачное хранилище

✅ **Удобный просмотр данных**
- Открываете таблицу в браузере
- Видите всех пользователей и их активность
- Можно фильтровать, сортировать

✅ **Легкое резервное копирование**
- Автоматические копии от Google
- Можно скачать в Excel/CSV

✅ **Бесплатно**
- Google Sheets бесплатен
- Лимит: 10 млн ячеек на таблицу
- Этого хватит на 100,000+ пользователей

✅ **Интеграция с другими сервисами**
- Можно подключить Google Apps Script
- Экспорт данных для аналитики
- Интеграция с N8N для автоматизации

---

## ⚠️ Лимиты и ограничения

**Google Sheets API лимиты:**
- 60 запросов в минуту на проект
- 100 запросов в 100 секунд на пользователя

**Для бота с большой нагрузкой:**
- Используйте кэширование
- Батчинг запросов
- Или переходите на PostgreSQL

**Оптимально для:**
- До 10,000 активных пользователей в месяц
- Умеренная нагрузка

---

## 🚀 Следующие шаги

После настройки Google Sheets:

1. ✅ **Протестируйте бота** - зарегистрируйтесь через `/start`
2. ✅ **Проверьте таблицу** - данные должны появиться в листе Users
3. ✅ **Протестируйте поиск** - проверьте что история сохраняется
4. 📊 **Настройте N8N** - автоматизация на основе данных из Google Sheets
5. 🤖 **Интегрируйте AI** - чтение предпочтений из Google Docs

---

## ❓ Частые вопросы

**Q: Где взять credentials.json?**
A: Создайте в Google Cloud Console → Service Account → Создать ключ → JSON

**Q: Как найти GOOGLE_SHEETS_ID?**
A: Это часть URL вашей таблицы между `/d/` и `/edit`

**Q: Service Account не видит таблицу**
A: Добавьте email из credentials.json в настройки доступа таблицы

**Q: Ошибка "403 Forbidden"**
A: Проверьте что Google Sheets API включен в Cloud Console

**Q: Медленная работа**
A: Используйте кэширование, батчинг запросов, или переходите на PostgreSQL

**Q: Можно ли использовать обычный Google аккаунт?**
A: Нет, нужен Service Account для автоматического доступа без авторизации

---

## 📞 Поддержка

Если что-то не работает:

1. Проверьте что `credentials.json` на месте
2. Проверьте что `GOOGLE_SHEETS_ID` в `.env` правильный
3. Проверьте что Service Account имеет доступ к таблице
4. Посмотрите логи бота при запуске
5. Проверьте что Google Sheets API включен в Cloud Console

---

**Готово!** После этой настройки бот будет хранить все данные в Google Sheets вместо PostgreSQL.

Это позволит:
- Удобно просматривать данные
- Легко интегрировать с N8N
- Работать без установки базы данных
- Получить автоматические бэкапы от Google

🎉 **Следуйте этой инструкции и присылайте мне credentials.json когда будет готов!**
