# 🤖 AI-персонализация с OpenAI для City Travel Bot

## Обзор

Интеграция OpenAI GPT-4 для создания умных, персонализированных рекомендаций на основе:
- Предпочтений пользователя из Google Docs
- Истории поисков
- Типа подписки (Free/Premium/VIP)
- Текущих событий в городе
- Локации отеля

---

## 🎯 Основные функции AI

### 1. Персональный маршрут от отеля

**Вход:**
- Город: Москва
- Даты: 15-17 января
- Отель: Метрополь, Театральный проезд, 2
- Бюджет: средний
- Предпочтения: культурные мероприятия, театр
- История: раньше искал Санкт-Петербург

**AI генерирует:**
```markdown
🎭 Ваш культурный маршрут в Москве (3 дня)

Старт от: Отель "Метрополь"
Бюджет: ~15,000₽

📍 День 1 - Классика (15 января)
━━━━━━━━━━━━━━━━━━━━━━━━━━━
09:00 🏨 Завтрак в отеле
      ↓ 5 мин пешком (350м)

10:00 🏛 Красная площадь + ГУМ
      Прогулка и шопинг
      ⏱ 2 часа | Бесплатно
      ↓ 15 мин пешком (1.1км)

12:30 🍽 Обед - Cafe Pushkin
      Русская кухня
      💰 ~2000₽ | ⏱ 1.5 часа
      ↓ 10 мин пешком (700м)

14:30 🎭 Большой Театр
      "Щелкунчик" - главная сцена
      💰 3500₽ | ⏱ 3 часа
      🎟 Билеты: bolshoi.ru
      ↓ 5 мин пешком (обратно в отель)

18:00 🏨 Отдых в отеле
      ↓ 20 мин на такси (5км)

20:00 🍷 Ужин - White Rabbit
      Панорамный вид на Москву
      💰 ~4500₽ | ⏱ 2 часа

💵 Итого день 1: 10,000₽
🚶 Пешком: 2.2км | 🚕 Такси: 5км

━━━━━━━━━━━━━━━━━━━━━━━━━━━

📍 День 2 - Искусство (16 января)
━━━━━━━━━━━━━━━━━━━━━━━━━━━
09:30 🏨 Выезд из отеля
      ↓ 25 мин метро (Театральная → Третьяковская)

10:30 🖼 Третьяковская галерея
      Русская живопись
      💰 500₽ | ⏱ 3 часа
      ↓ 15 мин пешком (900м)

14:00 🍽 Обед - Лавка-Лавка
      Фермерская кухня
      💰 ~1500₽ | ⏱ 1 час
      ↓ 30 мин метро + пешком

16:00 🎨 Garage Museum
      Современное искусство
      💰 600₽ | ⏱ 2 часа
      ↓ 10 мин пешком

18:30 🎸 Концерт в Crocus City Hall
      Симфонический оркестр
      💰 2500₽ | ⏱ 2.5 часа
      ↓ Такси обратно в отель

💵 Итого день 2: 5,100₽

━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 Персональные рекомендации:
• Вы любили Эрмитаж в СПб → вам понравится Третьяковка
• Средний бюджет → выбрал оптимальные рестораны
• Близко к отелю → первый день пешком
• Культура+театр → 2 спектакля + 2 музея

📱 Сохранить маршрут в PDF
🗺 Открыть на Яндекс.Картах
⭐️ Добавить в Избранное
```

---

## 🎯 Функция 2: Анализ предпочтений из Google Docs

### Структура документа пользователя

**Google Docs: "Мои предпочтения для путешествий"**

```
Общие предпочтения:
- Люблю культурные мероприятия
- Не люблю толпы
- Предпочитаю местную кухню
- Активный отдых днем, спокойный вечером

Бюджет:
- Готов потратить до 20,000₽ на поездку на выходные
- Экономлю на транспорте, трачу на еду и развлечения

Типы событий:
✅ Театр, опера, балет
✅ Музеи, галереи
✅ Концерты (классика, джаз)
❌ Клубы, ночная жизнь
❌ Экстрим

Города где был:
- Санкт-Петербург (5 раз) - очень понравилось
- Казань (2 раза) - интересно
- Сочи (1 раз) - слишком жарко

Хочу посетить:
- Нижний Новгород
- Екатеринбург
- Владивосток
```

### AI обрабатывает и сохраняет в Google Sheets (Preferences)

```javascript
{
  user_id: 123456,
  event_types: ['theater', 'museum', 'classical-music', 'jazz'],
  budget_level: 'medium',
  travel_style: 'cultural-relaxed',
  avoid_crowds: true,
  food_preference: 'local-cuisine',
  visited_cities: ['spb', 'kazan', 'sochi'],
  wishlist_cities: ['nizhny', 'ekaterinburg', 'vladivostok'],
  preferences_text: "Активный отдых днем, спокойный вечером. Любит местную кухню..."
}
```

---

## 🎯 Функция 3: Умные ответы на вопросы

### Примеры вопросов и AI-ответов

**Q: "Что посмотреть в Казани за 1 день?"**

AI анализирует:
- Пользователь уже был в Казани 2 раза
- Любит культурные места
- Не любит толпы

```
🏛 Казань за 1 день (с учетом того, что вы уже были)

Вы уже видели Кремль и Кул-Шариф, поэтому предлагаю:

1. 🕌 Старо-Татарская слобода (утро)
   Аутентичная архитектура, мало туристов

2. 🎨 Музей Константина Васильева (обед)
   Уникальная коллекция, спокойная атмосфера

3. 🍽 Обед в "Дом Татарской Кулинарии"
   Местная кухня (ваше предпочтение!)

4. 🏛 Национальный музей РТ (вечер)
   Если не были - must see для культурной программы

Избегаем: Баумана (слишком много людей по выходным)
```

**Q: "Куда сходить вечером в Москве?"**

AI проверяет:
- Время: 19:00
- Локация: рядом с отелем Метрополь
- Предпочтения: спокойный вечер

```
🌆 Вечерние варианты рядом с вашим отелем:

1. 🎭 Большой театр (5 мин пешком)
   Сегодня: "Спартак" 19:30
   Билеты от 2000₽

2. 🎹 Консерватория им. Чайковского (10 мин)
   Фортепианный концерт 20:00
   Билеты от 800₽

3. 🍷 Винный бар "Свои Люди" (7 мин)
   Спокойная атмосфера + местные вина

Учел: близко к отелю, спокойная атмосфера, ваш вкус к классике
```

---

## 🎯 Функция 4: Сравнение городов

**Q: "Что выбрать: Екатеринбург или Нижний Новгород?"**

AI анализирует оба города + предпочтения пользователя:

```
🏙 Екатеринбург vs Нижний Новгород

Для вас больше подойдет: Екатеринбург ⭐️

Почему:

📊 Культурная программа:
Екатеринбург: 🎭🎭🎭🎭 (4/5)
Нижний: 🎭🎭🎭 (3/5)
➜ В Екб: Театр Оперы, 20+ музеев, активная театральная жизнь

🍽 Кулинарная сцена:
Екатеринбург: 🍽🍽🍽🍽 (4/5)
Нижний: 🍽🍽🍽 (3/5)
➜ Больше ресторанов местной кухни

👥 Толпы туристов:
Екатеринбург: ⭐️⭐️ (меньше)
Нижний: ⭐️⭐️⭐️ (больше в центре)

💰 Средний чек:
Оба города: ~15,000₽ на выходные

🎯 Итого:
Екатеринбург даст вам больше культурных впечатлений
при меньшем количестве туристов.

📅 Лучшее время: май-сентябрь
🎭 Топ-3 события сейчас: [список]
```

---

## 🎯 Функция 5: Экстренные рекомендации

**Сценарий:** Событие отменено, нужна альтернатива

**Уведомление от бота:**
```
⚠️ Спектакль "Щелкунчик" в Большом отменен

🤖 AI подобрал замену:

1. 🎭 "Лебединое озеро" - Новая опера (19:00)
   Билеты от 1500₽ | 30 мин на метро

2. 🎼 Концерт в Зарядье (20:00)
   Современная классика | Билеты от 800₽

3. 🎨 Ночь в Пушкинском музее (19:00-23:00)
   Спецпрограмма | Вход 1000₽

Учтено: ваш бюджет, время, локация, стиль
```

---

## 🛠 Техническая реализация

### 1. OpenAI Service

```typescript
// src/services/openai.service.ts

import OpenAI from 'openai';
import { googleSheetsService } from './googleSheets.service.js';

class OpenAIService {
  private openai: OpenAI;

  constructor() {
    this.openai = new OpenAI({
      apiKey: process.env.OPENAI_API_KEY,
    });
  }

  /**
   * Генерация персонального маршрута
   */
  async generateRoute(params: {
    userId: number;
    city: string;
    dateFrom: Date;
    dateTo: Date;
    hotelAddress: string;
    events: any[];
  }): Promise<string> {
    // Получаем предпочтения пользователя
    const prefs = await googleSheetsService.getPreferences(params.userId);
    const history = await googleSheetsService.getUserSearchHistory(params.userId, 5);

    const prompt = `
Ты - эксперт по путешествиям. Создай детальный персональный маршрут.

Пользователь:
- Предпочтения: ${prefs?.travel_style || 'универсальный'}
- Бюджет: ${prefs?.budget_level || 'средний'}
- Любимые события: ${prefs?.event_types.join(', ') || 'любые'}
- Ранее искал: ${history.map(h => h.city).join(', ')}
- Дополнительно: ${prefs?.preferences_text || ''}

Поездка:
- Город: ${params.city}
- Даты: ${params.dateFrom.toLocaleDateString()} - ${params.dateTo.toLocaleDateString()}
- Старт от: ${params.hotelAddress}

Доступные события:
${params.events.map(e => `- ${e.title} (${e.category}, ${e.price}₽, ${e.event_date})`).join('\n')}

Требования:
1. Маршрут начинается от отеля
2. Включи расстояния и время в пути
3. Учти бюджет пользователя
4. Сделай акцент на предпочтениях
5. Добавь emoji для читаемости
6. Укажи цены на всё
7. Подведи итог бюджета за день

Формат: Markdown для Telegram
`;

    const completion = await this.openai.chat.completions.create({
      model: 'gpt-4-turbo-preview',
      messages: [
        {
          role: 'system',
          content: 'Ты - персональный помощник по планированию путешествий. Создаешь детальные, практичные маршруты с учетом предпочтений пользователя.'
        },
        {
          role: 'user',
          content: prompt
        }
      ],
      temperature: 0.7,
      max_tokens: 2000,
    });

    return completion.choices[0].message.content || '';
  }

  /**
   * Анализ предпочтений из Google Docs
   */
  async analyzePreferencesFromDocs(docsText: string): Promise<any> {
    const prompt = `
Проанализируй текст предпочтений пользователя и извлеки структурированные данные.

Текст:
${docsText}

Верни JSON:
{
  "event_types": ["theater", "museum", ...],
  "budget_level": "low" | "medium" | "high",
  "travel_style": "cultural" | "party" | "family" | "romantic" | "adventure",
  "avoid_crowds": true | false,
  "food_preference": "local" | "international" | "fine-dining" | "street-food",
  "visited_cities": ["city1", "city2"],
  "wishlist_cities": ["city1", "city2"],
  "summary": "краткое резюме предпочтений"
}
`;

    const completion = await this.openai.chat.completions.create({
      model: 'gpt-4-turbo-preview',
      messages: [{ role: 'user', content: prompt }],
      response_format: { type: 'json_object' },
    });

    return JSON.parse(completion.choices[0].message.content || '{}');
  }

  /**
   * Умный ответ на вопрос о городе
   */
  async answerCityQuestion(
    userId: number,
    question: string,
    city: string
  ): Promise<string> {
    const prefs = await googleSheetsService.getPreferences(userId);
    const history = await googleSheetsService.getUserSearchHistory(userId, 5);

    const prompt = `
Ответь на вопрос пользователя о городе ${city}.

Вопрос: ${question}

Контекст пользователя:
- Предпочтения: ${JSON.stringify(prefs)}
- История поисков: ${history.map(h => h.city).join(', ')}

Требования:
1. Персонализированный ответ с учетом предпочтений
2. Конкретные рекомендации (места, события, рестораны)
3. Практичная информация (цены, время, как добраться)
4. Emoji для структуры
5. Не более 500 слов

Формат: Markdown для Telegram
`;

    const completion = await this.openai.chat.completions.create({
      model: 'gpt-4-turbo-preview',
      messages: [{ role: 'user', content: prompt }],
      temperature: 0.8,
    });

    return completion.choices[0].message.content || '';
  }

  /**
   * Сравнение городов
   */
  async compareCities(
    userId: number,
    city1: string,
    city2: string
  ): Promise<string> {
    const prefs = await googleSheetsService.getPreferences(userId);

    const prompt = `
Сравни два города для пользователя: ${city1} vs ${city2}

Предпочтения пользователя:
${JSON.stringify(prefs)}

Критерии сравнения:
- Культурная программа
- Кулинарная сцена
- Количество туристов
- Бюджет
- Соответствие предпочтениям

Формат:
1. Краткий вывод какой город больше подходит
2. Сравнение по критериям
3. Итоговая рекомендация

Используй emoji и визуальные индикаторы (⭐️, 🎭, 💰)
`;

    const completion = await this.openai.chat.completions.create({
      model: 'gpt-4-turbo-preview',
      messages: [{ role: 'user', content: prompt }],
    });

    return completion.choices[0].message.content || '';
  }

  /**
   * Экстренная замена события
   */
  async findAlternativeEvent(
    userId: number,
    cancelledEvent: any,
    availableEvents: any[]
  ): Promise<string> {
    const prefs = await googleSheetsService.getPreferences(userId);

    const prompt = `
Событие отменено: ${cancelledEvent.title}

Найди замену из доступных событий:
${availableEvents.map((e, i) => `${i+1}. ${e.title} (${e.category}, ${e.price}₽, ${e.event_date})`).join('\n')}

Предпочтения: ${JSON.stringify(prefs)}

Критерии:
- Похожий тип события
- Подходящее время
- Соответствие бюджету
- Учет предпочтений

Предложи топ-3 варианта с обоснованием
`;

    const completion = await this.openai.chat.completions.create({
      model: 'gpt-4-turbo-preview',
      messages: [{ role: 'user', content: prompt }],
    });

    return completion.choices[0].message.content || '';
  }
}

export const openaiService = new OpenAIService();
```

---

## 💰 Стоимость использования OpenAI

### GPT-4 Turbo Pricing:
- **Input:** $10 / 1M tokens
- **Output:** $30 / 1M tokens

### Примерный расчет:

**Один запрос (генерация маршрута):**
- Input: ~1000 tokens (промпт + данные)
- Output: ~1500 tokens (маршрут)
- Стоимость: $0.01 + $0.045 = **$0.055 (~5₽)**

**При 1000 пользователей/месяц:**
- 1000 маршрутов × $0.055 = **$55/месяц (~5000₽)**

**Оптимизация:**
- Кэшировать частые запросы
- Использовать GPT-3.5-turbo для простых задач ($0.5/$1.5 за 1M tokens)
- Лимиты для Free пользователей

---

## 🚀 Интеграция в бота

### Новые команды:

```
/ai_route - Персональный маршрут от AI
/ask - Задать вопрос о городе
/compare - Сравнить города
/preferences - Настроить предпочтения
```

### Лимиты по тарифам:

| Тариф | AI запросов/день |
|-------|------------------|
| Free | 0 (недоступно) |
| Premium | 5 |
| VIP | безлимит |

---

## 📊 Метрики успеха

- % пользователей использующих AI
- Средняя оценка AI-рекомендаций
- Конверсия Free → Premium из-за AI
- Экономия времени пользователя
- NPS (Net Promoter Score)

---

**Следующий шаг:** Получить OpenAI API ключ и интегрировать в бота! 🤖
